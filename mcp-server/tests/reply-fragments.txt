# Caller-visible reply FRAGMENTS from the pure renderers. GENERATED — do not hand-edit:
#     UPDATE_TOOL_DESCRIPTIONS=1 cargo test -p jdwp-mcp --lib _snapshot
#
# This file exists because 163 substring assertions cannot see a REWORDING, and a reworded
# reply behind an unchanged tool name is the silent break docs/toolkit-contract.md is about.
# A diff here is a caller-visible change: read it, and put it in the release notes.
#
# `·` marks a newline so one fragment stays on one line and a diff stays readable.

## step_filter/none
·   No class filter: this steps into framework and JDK code as well as yours.

## step_filter/defaulted
·   Stepping OVER java.*, jdk.* (the default set) — pass exclude_classes:[] to step into everything

## step_filter/exclude-only
·   Stepping OVER java.* (your exclude_classes)

## step_filter/only-only
·   Stepping ONLY within com.example.*

## step_filter/both
·   Stepping OVER java.* (your exclude_classes)·   Stepping ONLY within com.example.*

## step_filter/session-exclude
·   ↪ Using the session step filter set at attach — this call named none. Pass exclude_classes or only_classes here to step under a different one.·   Stepping OVER org.jboss.* (the session's step_exclude_classes)

## step_filter/session-only
·   ↪ Using the session step filter set at attach — this call named none. Pass exclude_classes or only_classes here to step under a different one.·   Stepping OVER java.* (the default set) — pass exclude_classes:[] to step into everything·   Stepping ONLY within br.com.app.*

## step_filter/session-none
·   ↪ Using the session step filter set at attach — this call named none. Pass exclude_classes or only_classes here to step under a different one.·   No class filter: this steps into framework and JDK code as well as yours.

## session_step_filter/unset


## session_step_filter/exclude
·   👣 Session step filter: every debug.step_* call that names no filter of its own uses exclude_classes org.jboss.*, io.undertow.*. A step that names either exclude_classes or only_classes uses exactly what it names — this default is not merged into it.

## session_step_filter/empty-exclude
·   👣 Session step filter: every debug.step_* call that names no filter of its own uses NO exclusions (steps into everything). A step that names either exclude_classes or only_classes uses exactly what it names — this default is not merged into it.

## session_step_filter/only
·   👣 Session step filter: every debug.step_* call that names no filter of its own uses the built-in default exclusions, only within br.com.app.*. A step that names either exclude_classes or only_classes uses exactly what it names — this default is not merged into it.

## session_step_filter/both
·   👣 Session step filter: every debug.step_* call that names no filter of its own uses exclude_classes java.*, only within br.com.app.*. A step that names either exclude_classes or only_classes uses exactly what it names — this default is not merged into it.

## trace_frames/off


## trace_frames/zero
·   Caller frames: 0 (no caller frames are captured)

## trace_frames/some
·   Caller frames: 3

## trace_frames/noted
·   Caller frames: 3·   ⚠️  clamped from 99

## trace_exprs/none


## trace_exprs/one
·   Trace expr: pedido.total

## trace_exprs/several
·   Trace expr[0]: a·   Trace expr[1]: b·   Trace expr[2]: c

## trace_line/bare
#7 [bp_3] br.com.app.Pedido.reservar:-1 thread=0x1f4c

## trace_line/full
#7 [bp_3] br.com.app.Pedido.reservar:412 ← br.com.app.Controller.post:88 thread=0x1f4c field=Pedido.total {pedido=Pedido@0x7f3a} captured{val$tenant="acme"} | pedido.total => (int) 4200 ↻ rethrow of #2 (+3 more rethrow(s) collapsed)

## clamp_notes/none


## clamp_notes/first
first

## clamp_notes/second
second

## clamp_notes/both
first·   ⚠️  second

## session_default/unset


## session_default/set
·   👁 Session trace_expr: a, b — every stop point that names no trace_expr of its own records these, inside the window its hit already holds.

## session_default/clamped
·   👁 Session trace_expr: a — every stop point that names no trace_expr of its own records these, inside the window its hit already holds.·   ⚠️  dropped one

## took_session_default/no


## took_session_default/yes
·   ↪ Recording the session default trace_expr (a, b) — this stop point named none of its own. Pass trace_expr here to record something else, or set none on debug.attach to stop this.

## investigation_exposure
> ⚠️  **This report is NOT redacted, and reviewing it before you attach it anywhere is yours to do.**·>·> Trace snapshots hold whatever the debuggee's variables held at the hit: request and response payloads, bearer tokens in a header or a field, credentials sitting in a `byte[]`, customer records. This server alters none of it, because a pattern-based redactor that misses one secret is worse than no redactor — its output implies the file was cleaned. Nothing here has implied that.·>·> Until now this content went to one caller who had already seen it. A file on a ticket is a different exposure, and that is the change to weigh.·

## overridden_traces/none


## overridden_traces/one
·⚠️  SUSPEND POLICY OVERRIDDEN — these trace:true stop points freeze the WHOLE VM on every hit, because a JDWP composite carries one suspend policy for the entire event set (the strongest any member asked for) and something suspending is armed at the same location:·   Foo.java:12 — escalated by Bar.java:34·   debug.list_stop_points marks them too; clearing the suspending stop point restores snapshot-and-resume.·

## discarded_exits/no-filter


## discarded_exits/none-ran
 · exits discarded: 0·       ↳ so no method of this class returned at all while this was armed — here `Hits: 0` does mean the code did not run.

## discarded_exits/all-discarded
 · exits discarded: 3214·       ↳ this class IS executing: 3214 exit(s) of its OTHER methods arrived here and were dropped, because JDWP has no method-name modifier and a ClassMatch delivers every method of a matching class. So `Hits: 0` means `reservar` never returned — not that nothing ran. Those 3214 exit(s) are also what this stop point has cost the debuggee: each one is a notification the JVM raised and a packet it sent.

## discarded_exits/clean
 · exits discarded: 0

## discarded_exits/mixed
 · exits discarded: 3214·       ↳ 3214 exit(s) of other methods of this class were delivered and dropped as well, and the debuggee paid for each of them — JDWP has no method-name modifier, so a ClassMatch delivers every method. Narrowing `class_pattern` is the only thing that reduces it; the `method` filter is applied here, after the cost.

## no_session_refusal
No active debug session. Use debug.attach first.

## no_event_pump_refusal/ended
The JVM at localhost:8787 is gone — its event pump exited when the connection closed, so there is nothing left to arm a stop point in. Nothing was armed and nothing was sent. debug.list_sessions marks this session DEAD; debug.disconnect removes it, and debug.attach opens a session against a JVM that is running.

## no_event_pump_refusal/unstarted
The session attached to localhost:8787 has no event pump — nothing is reading this JVM's events, so a stop point armed on it would suspend the debuggee at its first hit and stay that way, whatever trace says. Nothing was armed and nothing was sent. Use debug.disconnect on it and debug.attach again; debug.list_sessions marks it NO EVENT PUMP.
